const { isBodyEmpty } = require("./isBodyEmpty");
const { isValidId } = require("./isValideId");
const { authenticate } = require("./authenticate");

module.exports = { isBodyEmpty, isValidId, authenticate };
